query GetCurrentLoggedUser {
    getCurrentLoggedUser {
        errors {
            method
            field
            message
        }
        user {
            id
            name
            email
            password
            role {
                name
            }
        }
    }
}
