query GetUserPendingInvitationsCount($id: String!) {
    getUserPendingInvitationsCount(id: $id) {
        errors {
            message
            method
            field
        }
        count
    }
}
