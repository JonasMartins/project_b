mutation CreatePost($options: PostValidator!, $file: Upload) {
    createPost(options: $options, file: $file) {
        errors {
            method
            message
            field
        }
        post {
            id
            body
            files
            creator {
                id
                name
            }
        }
    }
}
