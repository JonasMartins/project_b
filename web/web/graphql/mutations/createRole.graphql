mutation CreateRole($options: RoleValidator!) {
    createRole(options: $options) {
        errors {
            method
            message
            field
        }
        role {
            id
            name
            description
        }
    }
}
