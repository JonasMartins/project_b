mutation AddSeenNotification($userId: String!, $notificationId: String!) {
    addSeenNotification(userId: $userId, notificationId: $notificationId) {
        errors {
            message
        }
        done
    }
}
